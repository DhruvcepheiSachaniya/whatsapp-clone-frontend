import { Box, Typography } from "@mui/material";

const FirstPart: React.FC = () => {
  return (
    <Box>
      <Typography variant="h1">First Part</Typography>
    </Box>
  );
};

export default FirstPart;
